import csv

with open('Youtube.csv', mode='w') as csv_file:
    fieldnames = ['Name']
    writer = csv.writer(csv_file, fieldnames=fieldnames)
    
    writer.writeheader()
    writer.writerow({'Name': 'Code4Life'})
